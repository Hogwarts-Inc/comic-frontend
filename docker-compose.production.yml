version: '3.8'
services:
  front:
    container_name: production
    image: docker-production:latest
    restart: always #change if you don't want your container restarting
    environment:
      - NODE_ENV=production
    ports:
      - 80:3000
